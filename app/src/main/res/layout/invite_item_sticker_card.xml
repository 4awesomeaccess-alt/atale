<?xml version="1.0" encoding="utf-8"?>
<!-- res/layout/invite_item_sticker_card.xml
     Each sticker cell in RecyclerView grid -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_margin="@dimen/_5sdp"
    android:background="@drawable/bg_sticker_item_outer"
    android:elevation="3dp"
    android:padding="8dp">

    <!-- ── Inner white surface ── -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="6dp">
<!--      android:background="@drawable/bg_sticker_item_inner"-->
        <!-- Shimmer / placeholder while image loads -->
       <!-- <ImageView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:adjustViewBounds="true"
            android:src="@drawable/invite_loader_sticker"
            app:tint="#F5E6E8" />-->

        <!-- Actual sticker image -->
        <ImageView
            android:id="@+id/image"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:adjustViewBounds="true"
            android:scaleType="fitCenter" />

    </RelativeLayout>

    <!-- Premium lock badge — top-right corner -->
    <ImageView
        android:id="@+id/img_lock"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_alignParentEnd="true"
        android:layout_alignParentTop="true"
        android:layout_margin="2dp"
        android:background="@drawable/bg_lock_badge"
        android:padding="4dp"
        android:src="@drawable/ic_back"
        android:visibility="gone"
        app:tint="#FFFFFF" />

</RelativeLayout>
